CREATE OR REPLACE FUNCTION $APP_NAME$Views.SP_DCPGetStatus(
    pStatus CHAR(1)
)
RETURNS SETOF $APP_NAME$Views.Lookup_Status AS $$
BEGIN
    RETURN QUERY
	SELECT *
	FROM $APP_NAME$Views.Lookup_Status
	WHERE (Status = pStatus OR pStatus IS NULL)
	;	
END;
$$ LANGUAGE 'plpgsql';