CREATE OR REPLACE FUNCTION $APP_NAME$Views.SP_DCPDeactivateUser (
    pUserId INTEGER
) 
RETURNS VOID AS $$
BEGIN	

	-- Set DeactivatedTS value
	UPDATE $APP_NAME$.Users
	SET DeactivatedTS = current_timestamp,
		Is_Active = FALSE
	WHERE UserId = pUserId
	;
		
END;
$$ LANGUAGE 'plpgsql';